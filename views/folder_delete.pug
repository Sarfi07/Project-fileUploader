extends layout

block content
  .container.mx-auto.mt-10.max-w-lg
    .bg-gray-100.p-6.rounded-lg.shadow-md
      h1.text-3xl.font-bold.mb-6 Delete Confirmation

      // Confirmation Message
      p.text-gray-700.mb-4
        | Are you sure you want to delete the folder    
        strong #{folder.name}
        | ? This action cannot be undone.

      // Action Buttons
      .flex.space-x-4.justify-center
        a(href="/dashboard", class="inline-flex items-center px-6 py-3 bg-gray-500 text-white font-medium rounded-md hover:bg-gray-600 transition duration-300")
          | Cancel
        form(action=`/dashboard/folders/${folder.id}/delete`, method="POST")
          button(type="submit", class="inline-flex items-center px-6 py-3 bg-red-500 text-white font-medium rounded-md hover:bg-red-600 transition duration-300")
            | Delete
